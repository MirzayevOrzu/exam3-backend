/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<vocategory_id> } 
 */
exports.seed = async function(knex) {
  // Deletes ALL existing entries
  // await knex('categories_brands').del()/
  // await knex('categories_brands').insert([
  //   {category_id: 5, brand_id:26,},
  //   {category_id: 7, brand_id:27,},
  //   {category_id: 8, brand_id:28,}
  // ]);
};
